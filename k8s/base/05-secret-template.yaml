# k8s/base/05-secret-template.yaml
# DO NOT commit real values. Apply via:
#
#   kubectl create secret generic azure-openai-creds \
#     --namespace agentx-purple \
#     --from-literal=AZURE_OPENAI_KEY="$AZURE_OPENAI_KEY" \
#     --from-literal=AZURE_OPENAI_ENDPOINT="$AZURE_OPENAI_ENDPOINT"
#
# This file is a documentation stub. Including it in kustomization.yaml
# would force you to commit dummy values, so we leave it out of the
# resources list and document the imperative command instead.

apiVersion: v1
kind: Secret
metadata:
  name: azure-openai-creds
  namespace: agentx-purple
type: Opaque
stringData:
  AZURE_OPENAI_KEY: "REPLACE_ME"
  AZURE_OPENAI_ENDPOINT: "REPLACE_ME"
